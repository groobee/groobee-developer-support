# 프로젝트 전역 Gradle 설정
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX 사용
android.useAndroidX=true

# Kotlin 코드 스타일
kotlin.code.style=official

# 모듈별 R 클래스를 비전이(non-transitive)로 생성 (빌드 속도 최적화)
android.nonTransitiveRClass=true
